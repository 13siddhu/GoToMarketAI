services:
  - type: web
    name: lead-discovery-mvp
    env: docker
    region: ohio
    plan: starter
    healthCheckPath: /
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: SERP_API_KEY
        sync: false
